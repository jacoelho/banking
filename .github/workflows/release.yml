on:
  push:
    branches:
      - master

jobs:
  release-on-push:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - id: release
        uses: rymndhng/release-on-push-action@master
        with:
          bump_version_scheme: patch

      - name: Refresh pkg.go.dev
        run: curl -X POST "https://https://pkg.go.dev/fetch/${{ github.repository }}@${{ steps.release.outputs.tag_name }}
